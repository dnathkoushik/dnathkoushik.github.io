/** Shared visual vocabulary, so every component agrees on what "danger" means. */
export type Tone = 'neutral' | 'accent' | 'positive' | 'warning' | 'danger' | 'info'

export type Size = 'sm' | 'md' | 'lg'
